1. Одномерный массивы
- простой список элементов одного типа

1.1 Объявление и инициализация : тип имя_массива[размер];
пример:
int numbers[5]; //не инициализированный массив
float temps[] = {36.6, 37.0, 38.5};//Инициализированный массив

1.2 Доступ к элементам
- индексация начинается с нуля!
numbers[0] = 10; //первый элемент
numbers[4] = 50;//пятый элемент
пример:

#include <stdio.h>
int main() { 
 	int arr[5] = {10,20,30,40,50};
 	for (int i=0;i<5;i++){
 		printf("arr[%d] = %d\n", i, arr[i]);
	 }
    return 0;
}


2. Многомерные массивы
- массивы массивов

2.1 Двумерный массив (матрица) 
- строка х столбец
- тип имя [строки][столбцы];
пример: int matrix[2][3]; //2 строки, 3 столбца

2.2 Инициализация
int matrix[2][3] = {
	{1,2,3},//первая строка
	{4,5,6}//вторая строка
};
пример:

#include <stdio.h>
int main() { 
	int matrix[2][3] = {
		{1,2,3},
		{4,5,6}
	};
	for (int i=0;i<2;i++){
		for (int j=0;j<3;j++){
			printf("%d ", matrix[i][j]);
		}
		printf("\n");
	}	 
    return 0;
}

пример: сумма матрицы

#include <stdio.h>
int main() { 
	int matrix[2][3] = {{1, 2, 3}, {4, 5, 6}};
	int sum = 0;	
	for (int i=0;i<2;i++){
		for(int j=0;j<3;j++){
			sum += matrix[i][j];
		}
	}
	printf("Summa elementov = %d\n", sum);	 
    return 0;
}


